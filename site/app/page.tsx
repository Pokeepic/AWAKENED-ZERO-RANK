"use client";
import { useEffect, useState } from "react";
type S=any;
export default function Home(){const [s,setS]=useState<S>(null);useEffect(()=>{fetch("/data/observer-snapshot.json").then(r=>r.json()).then(setS)},[]);if(!s)return <main className="loading"><p>AUTHENTICATING CHRONICLE</p><h1>AWAKENED: ZERO RANK</h1></main>;const p=s.protagonist;const events=[...s.activity.recent_events].reverse().slice(0,6);return <main><header><b>AWAKENED <i>ZERO RANK</i></b><span>? LIVE OBSERVER ? READ ONLY</span></header><section className="hero"><div><small>REN'S CHRONICLE ? TOKYO, JAPAN</small><h1>A life unfolding<br/>without your hand.</h1><p>Rent is due. Gates are opening. Ren chooses what comes next.</p></div><aside>DAY<strong>{String(s.clock.day).padStart(3,"0")}</strong><em>{s.clock.slot}</em></aside></section><nav><span>{s.environment.weather} ? {s.environment.temperature_c}?C</span><span>{s.environment.season}</span><span>GATE ALERT {s.environment.gate_alert_level}</span><span>SEED {s.seed}</span></nav><div className="grid"><section className="profile"><label>CURRENT STATE <mark>RANK {p.hunter_rank}</mark></label><h2>{p.name}</h2><p>{p.location} ? {p.mood}</p><blockquote><small>ACTIVE INTENT</small><b>{p.current_goal}</b></blockquote>{Object.entries(p.resources).filter(([k])=>["health","energy","hunger","stress","morale"].includes(k)).map(([k,v]:any)=><div className="meter" key={k}><span>{k}<b>{v}</b></span><i><u style={{width:v+"%"}}/></i></div>)}<div className="money"><small>AVAILABLE</small><strong>?{p.resources.money.toLocaleString()}</strong></div></section><section className="events"><label>LATEST DECISIONS <span>{events.length} ENTRIES</span></label>{events.map((e:any)=><article key={e.day+e.slot+e.action}><time>D{e.day} ? {e.slot}</time><div><h3>{e.action}</h3><p>{e.outcome}</p></div></article>)}</section><section><label>HUNTER RECORD <span>{p.ability}</span></label><div className="stats">{[["Readiness",p.progression.combat_readiness],["Rank points",p.progression.rank_points],["Fitness",p.progression.fitness],["Knowledge",p.progression.knowledge]].map(x=><div key={x[0]}><b>{x[1]}</b><small>{x[0]}</small></div>)}</div></section><section className="story"><label>THREE-YEAR ARC <span>{s.story.completed_count}/{s.story.total_anchors}</span></label><h2>{s.story.next?.title||"Ending reached"}</h2><p>Fixed story anchor arrives on day {s.story.next?.day}.</p><strong>{s.story.next?.days_remaining}<small>DAYS REMAINING</small></strong></section><section className="people"><label>PEOPLE IN ORBIT <span>{s.relationships.length} KNOWN</span></label>{s.relationships.slice(0,4).map((r:any)=><article key={r.name}><i>{r.name.split(" ").map((x:string)=>x[0]).join("")}</i><div><b>{r.name}</b><small>{r.role}</small></div><strong>{r.trust}<small>trust</small></strong></article>)}</section><section><label>PORTAL LEDGER <span>{s.portals.discovered.length} FOUND</span></label>{s.portals.discovered.map((x:any)=><p className="portal" key={typeof x==="string"?x:x.name}>? {typeof x==="string"?x:x.name}</p>)}<code>VERIFIED ? {s.identity.digest.slice(0,16)}?</code></section></div><footer><b>AWAKENED: ZERO RANK</b><p>An autonomous life simulation. You watch. Ren decides.</p><small>NO CONTROL CAPABILITIES</small></footer></main>}
